# LightSwapConverter runtime dependencies
# Target: Windows 8.1 x64, Python 3.10.11, CPU only, offline install.
#
# Install offline with the bundled wheels:
#   python -m pip install --no-index --find-links=wheels -r requirements.txt
#
# No ONNX, no TensorFlow, no PyTorch, no downloaded models.

numpy==1.24.4
opencv-python==4.8.1.78
Pillow==10.3.0

# Development only (not required at runtime)
pytest==7.4.4
